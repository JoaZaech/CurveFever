<Page x:Class="CurveFever.Views.Game"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
      xmlns:local="clr-namespace:CurveFever.Views"
      xmlns:viewmodels="clr-namespace:CurveFever.ViewModels"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Game">
    <Page.DataContext>
        <viewmodels:GameViewModel/>
    </Page.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="241*"/>
            <ColumnDefinition Width="559*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="11*"/>
            <RowDefinition Height="39*"/>
        </Grid.RowDefinitions>
        <StackPanel Width="100" HorizontalAlignment="Left" Grid.RowSpan="2">
            <Label HorizontalContentAlignment="Center" Background="AliceBlue" Content="4" Margin="0,10,0,10"/>
            <StackPanel Height="76">
                <Label Content=""/>
                <Label Content="Player2"/>
            </StackPanel>
        </StackPanel>
        <Canvas Focusable="True" KeyUp="Page_KeyDown" Name="GameCanvas" Background="AliceBlue" Margin="100,0,0,0" Height="450" Width="700" Grid.RowSpan="2" Grid.ColumnSpan="2">
            <TextBlock Canvas.Top="10" Canvas.Left="20">Hello World!</TextBlock>
        </Canvas>
    </Grid>
</Page>
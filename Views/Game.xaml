<Page x:Class="CurveFever.Views.Game"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
      xmlns:local="clr-namespace:CurveFever.Views"
      xmlns:viewmodels="clr-namespace:CurveFever.ViewModels"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Game" Focusable="True" FocusVisualStyle="{x:Null}" KeyUp="Key_Click">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="241*"/>
            <ColumnDefinition Width="559*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="11*"/>
            <RowDefinition Height="39*"/>
        </Grid.RowDefinitions>
        <StackPanel Width="100" HorizontalAlignment="Left" Grid.RowSpan="2">
            <Label HorizontalContentAlignment="Center" Background="AliceBlue" Content="{Binding Test}" Margin="0,10,0,10"/>
            <StackPanel Height="76">
                <Label x:Name="MyLabel" Content=""/>
                <Label Content="Player2"/>
            </StackPanel>
        </StackPanel>
        <Canvas x:Name="GameCanvas" Background="AliceBlue" Margin="100,0,0,0" Height="450" Width="700" Grid.RowSpan="2" Grid.ColumnSpan="2">
            <Rectangle x:Name="MyRectangle" Width="50" Height="50" Fill="Blue"/>
        </Canvas>
    </Grid>
</Page>